function textLoader(text) {
  console.log(text.resource)
  // if (text) {
  //   text = text.split('\n').join(';')
  //   console.log('--- txt-loader output:', text)
  // }

  return text
}

module.exports = textLoader
